[project]
name = "alpamayo-closedloop-dashboard"
version = "0.1.0"
description = "Live dashboard for Alpamayo + Alpasim NRE closed-loop demo"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.36",
]

[tool.uv]
package = false
